package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Bento Grid Theme Colors
val BentoPrimary = Color(0xFF6750A4)       // Deep Purple / Brand Color
val BentoPrimaryLight = Color(0xFFEADDFF)  // Light Purple Badge Background
val BentoPrimaryDark = Color(0xFF21005D)   // Dark Violet for Text/Accents

// Card Colors (The classic Bento pastel shades)
val BentoPurpleCard = Color(0xFFE8DEF8)    // Soft lavender
val BentoGreyCard = Color(0xFFE7E0EC)      // Neutral soft grey-purple
val BentoBlueCard = Color(0xFFC2E7FF)      // Baby Blue (Paid status)
val BentoRedCard = Color(0xFFF2B8B5)       // Soft Pastel Red (Pending status)

val BentoBlueText = Color(0xFF001D35)      // Dark Blue Text for Paid card
val BentoRedText = Color(0xFF601410)       // Dark Red Text for Pending card
val BentoBorder = Color(0xFFCAC4D0)        // Material Outline Border

val BackgroundLight = Color(0xFFFDF8FF)    // Very soft lavender-tinted background
val SurfaceLight = Color(0xFFFFFFFF)       // White Surface
val OnBackgroundLight = Color(0xFF1C1B1F)  // Near Black
val OnSurfaceLight = Color(0xFF1C1B1F)     // Near Black

// Dark Theme Fallbacks (Styled to match Bento elegance but in darkmode)
val BentoPrimaryDarkTheme = Color(0xFFD0BCFF)
val BackgroundDark = Color(0xFF141218)
val SurfaceDark = Color(0xFF1D1B20)
val OnBackgroundDark = Color(0xFFE6E1E5)
val OnSurfaceDark = Color(0xFFE6E1E5)
